ush
become familiar with system calls.. mhanje nakki kaay mahit naai --check
hostname --check
parsing --check

8th la
I/O redirection --check 
execv --check

9th la
commands --within the shell 
cd --check
echo --check
logout --check	
nice --done command exec needs changes -- 11th 
pwd --get_cur_working_dir can be removed now
where --check

9th la
rc file ~/.ushrc --check
strings --check I guess :P
special chars and backslash --check I guess :P 
Signals --what all signals.. SIGQUIT (Ctrl+C) has to quit command sequence... 11th

10th la
Env Variables
setenv --done with setenv 
unsetenv --done with getenv

system calls
access --file permissions
chdir --change dir
nice --change nice value
signal --signal
